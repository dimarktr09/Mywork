<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Welcome to My Lounge!</title>
  <style>
    body {
      margin: 0;
      font-family: 'Arial', sans-serif;
      background-color: #222;
      overflow: hidden;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      color: #fff;
    }

    h1 {
      position: absolute;
      top: 20px;
      font-size: 2.5em;
      text-align: center;
      animation: fadeIn 2s forwards;
      opacity: 0;
    }

    @keyframes fadeIn {
      to { opacity: 1; }
    }

    /* Curtain container */
    .curtain-container {
      position: relative;
      width: 100%;
      height: 60%;
      display: flex;
      justify-content: center;
      align-items: center;
      overflow: hidden;
    }

    .curtain {
      position: absolute;
      width: 50%;
      height: 100%;
      background: #8B0000;
      top: 0;
      transition: transform 2s ease;
      z-index: 5;
    }

    .curtain.left { left: 0; }
    .curtain.right { right: 0; }

    .curtain.open.left { transform: translateX(-100%); }
    .curtain.open.right { transform: translateX(100%); }

    /* Couch */
    .couch {
      position: absolute;
      bottom: 20px;
      width: 300px;
      height: 150px;
      background: #654321;
      border-radius: 20px;
      display: flex;
      justify-content: center;
      align-items: center;
      cursor: pointer;
      transition: transform 0.3s;
    }

    .couch:hover {
      transform: translateY(-10px);
    }

    .couch::before {
      content: "🛋️ Sit here!";
      font-size: 1.5em;
    }

  </style>
</head>
<body>

  <h1>Welcome to the Cozy Lounge!</h1>

  <div class="curtain-container">
    <div class="curtain left"></div>
    <div class="curtain right"></div>
    <div class="couch" onclick="sitOnCouch()"></div>
  </div>

  <script>
    // Open curtains after 1 second
    setTimeout(() => {
      document.querySelector('.curtain.left').classList.add('open');
      document.querySelector('.curtain.right').classList.add('open');
    }, 1000);

    function sitOnCouch() {
      alert("You are now sitting on the couch! 🛋️ Relax and enjoy!");
    }
  </script>

</body>
</html>
